# Teams Bot — Daily Menu Card Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Bot gửi Adaptive Card menu vào Teams group chat mỗi 7:30 sáng — user bấm chọn món ngay trong chat, không cần mở web.

**Architecture:** Bot Express route nhận `invoke` từ Teams, lấy `activity.from.name` làm `person_name`, gọi thẳng vào DB logic (không HTTP round-trip nội bộ). Conversation reference (serviceUrl + conversationId) lưu vào SQLite qua migration. Cron job 7:30 sáng tích hợp vào `server/src/cron.js` đã có.

**Tech Stack:** `botbuilder` (Bot Framework SDK v4), `better-sqlite3`, `node-cron`, Express ESM

---

## File Map

| File | Vai trò |
|------|---------|
| `server/src/db/schema.js` | Thêm table `bot_conversation` |
| `server/src/db/index.js` | Thêm migration cho `bot_conversation` |
| `server/src/teams/cardBuilder.js` | Build Adaptive Card JSON từ menu items |
| `server/src/routes/bot.js` | Express route `/api/bot` — xử lý Teams activities |
| `server/src/cron.js` | Thêm cron 7:30am gửi daily menu card |
| `server/index.js` | Mount `/api/bot` route |
| `server/package.json` | Thêm `botbuilder` dependency |
| `teams-app/manifest.json` | Teams App manifest để sideload |
| `.env.example` | Document env vars cần thiết |

---

## Task 1: Install botbuilder & add DB schema

**Files:**
- Modify: `server/package.json`
- Modify: `server/src/db/schema.js`
- Modify: `server/src/db/index.js`

- [ ] **Step 1: Install botbuilder**

```bash
cd server && npm install botbuilder
```

Expected: `botbuilder` xuất hiện trong `node_modules/`, không có peer dep errors.

- [ ] **Step 2: Thêm table `bot_conversation` vào schema**

Trong `server/src/db/schema.js`, thêm vào cuối string `CREATE_TABLES`:

```js
export const CREATE_TABLES = `
  -- ... (giữ nguyên các table cũ) ...

  CREATE TABLE IF NOT EXISTS bot_conversation (
    id INTEGER PRIMARY KEY CHECK (id = 1),
    service_url TEXT NOT NULL,
    conversation_id TEXT NOT NULL,
    tenant_id TEXT,
    updated_at TEXT NOT NULL DEFAULT (datetime('now'))
  );
`;
```

> Table dùng `CHECK (id = 1)` để chỉ lưu 1 row duy nhất (singleton pattern).

- [ ] **Step 3: Thêm migration trong `server/src/db/index.js`**

Sau đoạn migration cuối cùng (idx_menu_addons_unique), thêm:

```js
// Migration: bot_conversation table (created via CREATE TABLE IF NOT EXISTS in schema)
// No column migration needed — table is new
```

> Migration không cần vì `CREATE TABLE IF NOT EXISTS` đã handle trong `CREATE_TABLES`.

- [ ] **Step 4: Commit**

```bash
cd server && git add package.json package-lock.json src/db/schema.js src/db/index.js
git commit -m "feat: install botbuilder, add bot_conversation table"
```

---

## Task 2: Adaptive Card Builder

**Files:**
- Create: `server/src/teams/cardBuilder.js`
- Create: `server/src/teams/cardBuilder.test.js`

- [ ] **Step 1: Viết failing test**

Tạo `server/src/teams/cardBuilder.test.js`:

```js
import { describe, it, expect } from 'vitest';
import { buildMenuCard, buildConfirmedCard } from './cardBuilder.js';

const sampleItems = [
  { id: 1, name: 'Cơm sườn', price: 35000, category: 'Cơm' },
  { id: 2, name: 'Cơm gà', price: 30000, category: 'Cơm' },
  { id: 3, name: 'Bún bò', price: 30000, category: 'Bún' },
];

describe('buildMenuCard', () => {
  it('returns valid Adaptive Card with correct type', () => {
    const card = buildMenuCard(sampleItems);
    expect(card.type).toBe('AdaptiveCard');
    expect(card['$schema']).toBe('http://adaptivecards.io/schemas/adaptive-card.json');
    expect(card.version).toBe('1.4');
  });

  it('groups items by category', () => {
    const card = buildMenuCard(sampleItems);
    const bodyText = JSON.stringify(card.body);
    expect(bodyText).toContain('Cơm');
    expect(bodyText).toContain('Bún');
  });

  it('creates one Action.Submit per menu item', () => {
    const card = buildMenuCard(sampleItems);
    const actions = card.actions;
    expect(actions).toHaveLength(3);
    expect(actions[0].type).toBe('Action.Submit');
    expect(actions[0].data.menu_item_id).toBe(1);
  });

  it('formats price in Vietnamese dong', () => {
    const card = buildMenuCard(sampleItems);
    const bodyText = JSON.stringify(card.body);
    expect(bodyText).toContain('35k');
  });
});

describe('buildConfirmedCard', () => {
  it('shows confirmation message with person name and item name', () => {
    const card = buildConfirmedCard('An', 'Cơm sườn');
    const bodyText = JSON.stringify(card.body);
    expect(bodyText).toContain('An');
    expect(bodyText).toContain('Cơm sườn');
    expect(bodyText).toContain('✅');
  });
});
```

- [ ] **Step 2: Chạy test để verify fail**

```bash
cd server && npm test -- src/teams/cardBuilder.test.js
```

Expected: FAIL — "Cannot find module './cardBuilder.js'"

- [ ] **Step 3: Implement cardBuilder.js**

Tạo `server/src/teams/cardBuilder.js`:

```js
export function buildMenuCard(menuItems) {
  const today = new Date().toLocaleDateString('vi-VN', {
    weekday: 'long', day: '2-digit', month: '2-digit',
    timeZone: 'Asia/Ho_Chi_Minh',
  });

  // Group by category
  const byCategory = {};
  for (const item of menuItems) {
    const cat = item.category || 'Khác';
    if (!byCategory[cat]) byCategory[cat] = [];
    byCategory[cat].push(item);
  }

  const body = [
    {
      type: 'TextBlock',
      text: `🍱 Menu hôm nay — ${today}`,
      weight: 'Bolder',
      size: 'Medium',
      wrap: true,
    },
  ];

  for (const [category, items] of Object.entries(byCategory)) {
    body.push({ type: 'TextBlock', text: category, weight: 'Bolder', spacing: 'Medium' });
    body.push({
      type: 'ColumnSet',
      columns: items.map(item => ({
        type: 'Column',
        width: 'auto',
        items: [{
          type: 'TextBlock',
          text: `${item.name} — ${Math.round(item.price / 1000)}k`,
          size: 'Small',
          wrap: true,
        }],
      })),
    });
  }

  const actions = menuItems.map(item => ({
    type: 'Action.Submit',
    title: `${item.name} ${Math.round(item.price / 1000)}k`,
    data: { menu_item_id: item.id, action: 'order' },
  }));

  return {
    type: 'AdaptiveCard',
    '$schema': 'http://adaptivecards.io/schemas/adaptive-card.json',
    version: '1.4',
    body,
    actions,
  };
}

export function buildConfirmedCard(personName, itemName) {
  return {
    type: 'AdaptiveCard',
    '$schema': 'http://adaptivecards.io/schemas/adaptive-card.json',
    version: '1.4',
    body: [
      {
        type: 'TextBlock',
        text: `✅ **${personName}** đã chọn **${itemName}**`,
        wrap: true,
        size: 'Medium',
      },
      {
        type: 'TextBlock',
        text: 'Vào web để thêm món phụ hoặc huỷ order.',
        wrap: true,
        size: 'Small',
        isSubtle: true,
      },
    ],
  };
}
```

- [ ] **Step 4: Chạy test để verify pass**

```bash
cd server && npm test -- src/teams/cardBuilder.test.js
```

Expected: PASS — 5 tests passed

- [ ] **Step 5: Commit**

```bash
git add server/src/teams/cardBuilder.js server/src/teams/cardBuilder.test.js
git commit -m "feat: add Teams Adaptive Card builder with tests"
```

---

## Task 3: Bot Route

**Files:**
- Create: `server/src/routes/bot.js`
- Modify: `server/index.js`

- [ ] **Step 1: Tạo file .env.example để document env vars**

Tạo (hoặc append) `.env.example` ở root:

```bash
# Teams Bot
TEAMS_APP_ID=your-azure-bot-app-id
TEAMS_APP_SECRET=your-azure-bot-app-secret
```

- [ ] **Step 2: Tạo `server/src/routes/bot.js`**

```js
import { CloudAdapter, ConfigurationBotFrameworkAuthentication } from 'botbuilder';
import { Router } from 'express';
import { getDb } from '../db/index.js';
import { broadcast } from '../services/sse.js';
import { buildConfirmedCard } from '../teams/cardBuilder.js';

const auth = new ConfigurationBotFrameworkAuthentication({
  MicrosoftAppId: process.env.TEAMS_APP_ID,
  MicrosoftAppPassword: process.env.TEAMS_APP_SECRET,
  MicrosoftAppType: 'MultiTenant',
});

const adapter = new CloudAdapter(auth);

adapter.onTurnError = async (context, error) => {
  console.error('[bot] onTurnError:', error);
  await context.sendActivity('Đã xảy ra lỗi. Vui lòng thử lại.');
};

export const botRouter = Router();

botRouter.post('/', (req, res) => {
  adapter.process(req, res, async (context) => {
    const { type, from, value } = context.activity;

    // Bot added to channel/chat — save conversation reference
    if (type === 'conversationUpdate') {
      const ref = context.activity;
      const db = getDb();
      db.prepare(`
        INSERT INTO bot_conversation (id, service_url, conversation_id, tenant_id, updated_at)
        VALUES (1, ?, ?, ?, datetime('now'))
        ON CONFLICT(id) DO UPDATE SET
          service_url = excluded.service_url,
          conversation_id = excluded.conversation_id,
          tenant_id = excluded.tenant_id,
          updated_at = excluded.updated_at
      `).run(
        ref.serviceUrl,
        ref.conversation.id,
        ref.conversation.tenantId ?? null,
      );
      console.log('[bot] Conversation reference saved');
      return;
    }

    // User tapped a button on the card
    if (type === 'invoke' && value?.action === 'order') {
      const personName = from.name;
      const menuItemId = value.menu_item_id;

      if (!personName || !menuItemId) {
        await context.sendActivity('Thiếu thông tin order.');
        return;
      }

      const db = getDb();
      const today = new Date().toISOString().slice(0, 10);

      const isLocked = db.prepare(
        'SELECT is_locked FROM daily_menu WHERE date = ? LIMIT 1'
      ).get(today)?.is_locked === 1;

      if (isLocked) {
        await context.sendActivity('⛔ Order hôm nay đã khoá rồi.');
        return;
      }

      const menuItem = db.prepare('SELECT * FROM menu_items WHERE id = ?').get(menuItemId);
      if (!menuItem) {
        await context.sendActivity('Món này không tồn tại.');
        return;
      }

      // Replace existing order for this person today
      db.transaction(() => {
        const existing = db.prepare(
          'SELECT id FROM orders WHERE person_name = ? AND date = ?'
        ).all(personName, today);
        for (const o of existing) {
          db.prepare('DELETE FROM order_addons WHERE order_id = ?').run(o.id);
          db.prepare('DELETE FROM orders WHERE id = ?').run(o.id);
        }
        db.prepare(
          'INSERT INTO orders (person_name, menu_item_id, date) VALUES (?, ?, ?)'
        ).run(personName, menuItemId, today);
      })();

      // Broadcast to SSE so web app updates in realtime
      const updatedOrders = db.prepare(
        `SELECT o.id, o.person_name, mi.name as item_name, mi.price, mi.category
         FROM orders o JOIN menu_items mi ON mi.id = o.menu_item_id
         WHERE o.person_name = ? AND o.date = ?`
      ).all(personName, today);

      broadcast('order_submitted', { person_name: personName, orders: updatedOrders });

      // Update the card in-place
      const confirmedCard = buildConfirmedCard(personName, menuItem.name);
      await context.updateActivity({
        id: context.activity.replyToId,
        type: 'message',
        attachments: [{
          contentType: 'application/vnd.microsoft.card.adaptive',
          content: confirmedCard,
        }],
      });
    }
  });
});
```

- [ ] **Step 3: Mount bot route trong `server/index.js`**

Thêm 2 dòng vào `server/index.js`:

```js
import { botRouter } from './src/routes/bot.js';
// ...
app.use('/api/bot', botRouter);
```

File đầy đủ sau khi sửa:

```js
import express from 'express';
import cors from 'cors';
import { menuRouter } from './src/routes/menu.js';
import { ordersRouter } from './src/routes/orders.js';
import { debtsRouter } from './src/routes/debts.js';
import { eventsRouter } from './src/routes/events.js';
import { webhookRouter } from './src/routes/webhook.js';
import { botRouter } from './src/routes/bot.js';
import { startCron } from './src/cron.js';

const app = express();
app.use(cors());
app.use(express.json());

app.use('/api/menu', menuRouter);
app.use('/api/orders', ordersRouter);
app.use('/api/debts', debtsRouter);
app.use('/api/events', eventsRouter);
app.use('/api/webhook', webhookRouter);
app.use('/api/bot', botRouter);

startCron();

const PORT = process.env.PORT || 3001;
app.listen(PORT, () => console.log(`LunchTime API running on :${PORT}`));

export default app;
```

- [ ] **Step 4: Verify server starts without error**

```bash
cd server && TEAMS_APP_ID=test TEAMS_APP_SECRET=test node index.js &
sleep 2 && curl -s http://localhost:3001/api/menu/today | head -c 100
kill %1
```

Expected: JSON response từ menu API, không có crash.

- [ ] **Step 5: Commit**

```bash
git add server/src/routes/bot.js server/index.js .env.example
git commit -m "feat: add Teams bot route, mount on /api/bot"
```

---

## Task 4: Cron Job — Send Daily Menu Card

**Files:**
- Modify: `server/src/cron.js`

- [ ] **Step 1: Thêm hàm `sendDailyMenuCard` vào `server/src/cron.js`**

File đầy đủ sau khi sửa:

```js
import cron from 'node-cron';
import { CloudAdapter, ConfigurationBotFrameworkAuthentication, ConversationReference } from 'botbuilder';
import { getDb } from './db/index.js';
import { broadcast } from './services/sse.js';
import { buildMenuCard } from './teams/cardBuilder.js';

function makeAdapter() {
  return new CloudAdapter(
    new ConfigurationBotFrameworkAuthentication({
      MicrosoftAppId: process.env.TEAMS_APP_ID,
      MicrosoftAppPassword: process.env.TEAMS_APP_SECRET,
      MicrosoftAppType: 'MultiTenant',
    })
  );
}

async function sendDailyMenuCard() {
  const db = getDb();
  const today = new Date().toISOString().slice(0, 10);

  const ref = db.prepare('SELECT * FROM bot_conversation WHERE id = 1').get();
  if (!ref) {
    console.log('[cron] No bot conversation reference saved yet — skipping menu send');
    return;
  }

  const menuItems = db.prepare(`
    SELECT mi.id, mi.name, mi.price, mi.category
    FROM daily_menu dm
    JOIN menu_items mi ON mi.id = dm.menu_item_id
    WHERE dm.date = ? AND dm.is_available = 1
    ORDER BY mi.category, mi.name
  `).all(today);

  if (menuItems.length === 0) {
    console.log('[cron] No menu for today — skipping Teams card send');
    return;
  }

  const card = buildMenuCard(menuItems);
  const adapter = makeAdapter();

  const conversationRef = {
    serviceUrl: ref.service_url,
    conversation: { id: ref.conversation_id, tenantId: ref.tenant_id },
    bot: { id: process.env.TEAMS_APP_ID },
  };

  await adapter.continueConversationAsync(
    process.env.TEAMS_APP_ID,
    conversationRef,
    async (context) => {
      await context.sendActivity({
        attachments: [{
          contentType: 'application/vnd.microsoft.card.adaptive',
          content: card,
        }],
      });
    }
  );

  console.log(`[cron] Menu card sent to Teams for ${today}`);
}

export function startCron() {
  // Lock orders at 11am Mon-Fri
  cron.schedule('0 11 * * 1-5', () => {
    const db = getDb();
    const today = new Date().toISOString().slice(0, 10);
    db.prepare('UPDATE daily_menu SET is_locked = 1 WHERE date = ?').run(today);
    broadcast('order_locked', { date: today, reason: 'auto' });
    console.log(`[cron] Orders locked for ${today}`);
  }, { timezone: 'Asia/Ho_Chi_Minh' });

  // Send daily menu card to Teams at 7:30am Mon-Fri
  cron.schedule('30 7 * * 1-5', () => {
    sendDailyMenuCard().catch(err => console.error('[cron] sendDailyMenuCard error:', err));
  }, { timezone: 'Asia/Ho_Chi_Minh' });
}
```

- [ ] **Step 2: Commit**

```bash
git add server/src/cron.js
git commit -m "feat: add cron job to send daily menu card to Teams at 7:30am"
```

---

## Task 5: Teams App Manifest

**Files:**
- Create: `teams-app/manifest.json`
- Create: `teams-app/color.png` (192x192px, icon màu)
- Create: `teams-app/outline.png` (32x32px, icon trắng nền trong suốt)

- [ ] **Step 1: Tạo thư mục và manifest**

```bash
mkdir -p teams-app
```

Tạo `teams-app/manifest.json`:

```json
{
  "$schema": "https://developer.microsoft.com/json-schemas/teams/v1.17/MicrosoftTeams.schema.json",
  "manifestVersion": "1.17",
  "version": "1.0.0",
  "id": "$TEAMS_APP_ID",
  "developer": {
    "name": "LunchTime",
    "websiteUrl": "https://your-domain.com",
    "privacyUrl": "https://your-domain.com",
    "termsOfUseUrl": "https://your-domain.com"
  },
  "name": {
    "short": "LunchTime",
    "full": "LunchTime — Order cơm trưa"
  },
  "description": {
    "short": "Order cơm trưa ngay trong Teams",
    "full": "Bot gửi menu hàng ngày, chọn món bằng 1 click ngay trong chat."
  },
  "icons": {
    "color": "color.png",
    "outline": "outline.png"
  },
  "accentColor": "#F97316",
  "bots": [
    {
      "botId": "$TEAMS_APP_ID",
      "scopes": ["team", "groupChat"],
      "isNotificationOnly": false
    }
  ],
  "permissions": ["identity", "messageTeamMembers"],
  "validDomains": ["your-domain.com"]
}
```

> Thay `$TEAMS_APP_ID` bằng App ID thật và `your-domain.com` bằng domain server.

- [ ] **Step 2: Tạo icon placeholder**

Tạo 2 file PNG tạm (bạn có thể thay bằng icon thật sau):

```bash
# Cần có ImageMagick hoặc dùng bất kỳ 2 file PNG đúng kích thước
# Nếu không có ImageMagick, copy bất kỳ PNG nào vào teams-app/ và rename
convert -size 192x192 xc:"#F97316" -fill white -gravity Center \
  -font DejaVu-Sans-Bold -pointsize 60 -annotate 0 "LT" teams-app/color.png
convert -size 32x32 xc:transparent -fill white -gravity Center \
  -font DejaVu-Sans-Bold -pointsize 14 -annotate 0 "LT" teams-app/outline.png
```

- [ ] **Step 3: Đóng gói thành .zip để sideload**

```bash
cd teams-app && zip LunchTime.zip manifest.json color.png outline.png
```

- [ ] **Step 4: Hướng dẫn sideload vào Teams**

1. Mở Microsoft Teams
2. Vào **Apps** → **Manage your apps** → **Upload an app** → **Upload a custom app**
3. Chọn file `teams-app/LunchTime.zip`
4. Add bot vào group chat/channel mà team dùng để order
5. Bot sẽ nhận `conversationUpdate` và lưu conversation reference vào DB

- [ ] **Step 5: Commit**

```bash
git add teams-app/
git commit -m "feat: add Teams app manifest for sideloading"
```

---

## Task 6: Azure Bot Registration (Manual Steps)

> Bước này thực hiện trên Azure Portal, không phải code.

- [ ] **Step 1: Tạo Azure Bot resource**

1. Vào [portal.azure.com](https://portal.azure.com)
2. Search "Azure Bot" → Create
3. Bot handle: `lunchtime-bot`
4. Pricing: **Free (F0)**
5. App type: **Multi-tenant**
6. Tạo new Microsoft App ID → ghi lại App ID

- [ ] **Step 2: Lấy App Secret**

1. Trong Azure Bot resource → Configuration → Manage Password
2. New client secret → ghi lại secret (chỉ hiện 1 lần)

- [ ] **Step 3: Set messaging endpoint**

Trong Azure Bot → Configuration:
```
Messaging endpoint: https://your-domain.com/api/bot
```

- [ ] **Step 4: Enable Teams channel**

Trong Azure Bot → Channels → Add Microsoft Teams channel → Save

- [ ] **Step 5: Thêm env vars vào server**

Thêm vào `.env` (file thật, không commit):

```
TEAMS_APP_ID=<app-id-từ-azure>
TEAMS_APP_SECRET=<secret-từ-azure>
```

Restart server:
```bash
pm2 restart all
```

---

## Task 7: Xoá Import Menu khỏi UserSidebar (Minimal scope từ brainstorm)

**Files:**
- Modify: `client/src/components/UserSidebar.jsx`

- [ ] **Step 1: Xoá dòng Import menu khỏi NAV array**

Trong `client/src/components/UserSidebar.jsx`, sửa mảng `NAV`:

```js
const NAV = [
  { to: '/', icon: UtensilsCrossed, label: 'Order hôm nay' },
  { to: '/summary', icon: ClipboardList, label: 'Tổng hợp' },
  { to: '/debt', icon: CreditCard, label: 'Công nợ' },
];
```

Xoá import `Download` khỏi lucide-react:

```js
import { UtensilsCrossed, ClipboardList, CreditCard } from 'lucide-react';
```

- [ ] **Step 2: Commit**

```bash
git add client/src/components/UserSidebar.jsx
git commit -m "feat: remove Import menu from user sidebar"
```

---

## Self-Review

**Spec coverage:**
- ✅ Azure Bot registration → Task 6
- ✅ Bot route với conversationUpdate + invoke → Task 3
- ✅ Adaptive Card builder → Task 2
- ✅ Cron 7:30am → Task 4
- ✅ Teams manifest → Task 5
- ✅ Security (JWT verify via CloudAdapter) → Task 3, Step 2 (CloudAdapter tự verify)
- ✅ SSE broadcast → Task 3 (bot route gọi `broadcast`)
- ✅ Xoá Import menu → Task 7

**Không có placeholder hay TBD.** Type/method names nhất quán giữa các task: `buildMenuCard`, `buildConfirmedCard`, `botRouter`, `sendDailyMenuCard`.
